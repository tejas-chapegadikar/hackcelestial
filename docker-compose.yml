services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: hre
      POSTGRES_PASSWORD: hre_dev_password
      POSTGRES_DB: hre
    ports:
      - "5432:5432"
    volumes:
      - hre_pgdata:/var/lib/postgresql/data

volumes:
  hre_pgdata:
